# MCP PDF Server

Servidor MCP local (Node.js ESM) que genera PDFs A4 con Puppeteer.

## Tools

- `generate_pdf_from_html` — HTML string + filename
- `generate_pdf_from_url` — URL + filename

Los archivos se guardan en `output/`.

## Install

```bash
cd mcp-pdf-server
npm install
```
